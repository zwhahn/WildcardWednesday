{
  "name": "wildcardwednesday",
  "version": "1.0.0",
  "description": "Find a random place for lunch around work and allow people to give a rating to track quality",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && mkdir -p ./dist && cp -r ./src/* ./dist/ && rm ./dist/input.css && tailwindcss -i ./src/input.css -o ./dist/output.css --minify"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "daisyui": "^4.12.23"
  }
}
